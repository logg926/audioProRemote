#/bin/bash

set -e

rsync -avu --delete --delete-excluded \
	--exclude=/.git \
	--exclude=/node_modules \
	--exclude=/worker/out \
	--exclude=/worker/deps/clang-fuzzer \
	--exclude=/worker/fuzzer/new-corpus \
	--exclude=/worker/fuzzer/reports \
	--filter='protect /worker/out' \
	--filter='protect /node_modules' \
	./ vhost1-deploy:/home/deploy/src/mediasoup-v2-deploy
